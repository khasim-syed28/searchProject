# searchProject
Built a Profile Search Application using HTML, CSS, and JavaScript.  Features:  Dynamic user cards Live search with filtering "No user found" state Add new users through a form Dynamic DOM manipulation using JavaScript  While building it, I practiced working with arrays, objects, event listeners, filtering, and updating the UI dynamically.
